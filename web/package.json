{
  "name": "pewpew-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:demo": "tsc --noEmit && vite build --mode demo",
    "preview": "vite preview"
  },
  "dependencies": {
    "pixi.js": "^8.6.6",
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@types/three": "^0.180.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  },
  "description": "Orbital Command — real-time UniFi syslog space visualizer + generative soundtrack (PixiJS v8)",
  "license": "MIT"
}
